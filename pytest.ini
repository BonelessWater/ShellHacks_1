[pytest]
markers =
    integration: marks tests as integration (deselect with -m "not integration")
    slow: marks slow tests
    requires_gcp: marks tests that require access to GCP resources
addopts = -q
asyncio_mode = auto
